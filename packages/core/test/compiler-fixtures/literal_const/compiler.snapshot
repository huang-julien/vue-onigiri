import { toDisplayString as _toDisplayString } from "vue";

export function renderOnigiri(_ctx, __instance) {
  return [
    0,
    "div",
    { "data-version": _ctx.VERSION, "data-enabled": _ctx.ENABLED },
    [[2, _toDisplayString(_ctx.TITLE)]],
  ];
}
